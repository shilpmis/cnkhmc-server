import { BaseSeeder } from '@adonisjs/lucid/seeders'

export default class extends BaseSeeder {
  async run() {
    // InCNKHMC ERP, roles and permissions are managed via JSON column in role_master table.
    // This seeder is a placeholder and does nothing.
  }
}

